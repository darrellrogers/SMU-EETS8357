# sudo apt-get update# sudo apt-get upgrade -y # sudo su# apt -y install docker.io conntrack # snap install kubectl --classic# wget https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64 -O minikube# chmod 755 minikube# mv minikube /usr/local/bin/ # swapoff -a# systemctl status docker# git clone https://github.com/Mirantis/cri-dockerd.git=======================================CHANGES BEGIN HERE:### = no longer valid commands; do not use#### wget https://storage.googleapis.com/golang/getgo/installer_linux ### INSERT THESE FOLLOWING COMMANDS IN PLACE OF REMOVED COMMANDS# wget https://go.dev/dl/go1.23.3.linux-amd64.tar.gz# sudo rm -rf /usr/local/go# sudo tar -C /usr/local -xzf go1.23.3.linux-amd64.tar.gz# echo "export PATH=$PATH:/usr/local/go/bin" >> ~/.bashrc# source ~/.bashrc### sudo rm -### chmod +x ./installer_linux### ./installer_linux### source ~/.bash_profile =========================RESUME:# cd cri-dockerd# mkdir bin# go build -o bin/cri-dockerd # mkdir -p /usr/local/bin# install -o root -g root -m 0755 bin/cri-dockerd /usr/local/bin/cri-dockerd # cp -a packaging/systemd/* /etc/systemd/system# sed -i -e 's,/usr/bin/cri-dockerd,/usr/local/bin/cri-dockerd,' /etc/systemd/system/cri- docker.service# systemctl daemon-reload# systemctl enable cri-docker.service# systemctl enable --now cri-docker.socket # sudo sysctl fs.protected_regular=0# wget https://github.com/kubernetes-sigs/cri-tools/releases/download/v1.25.0/crictl-v1.25.0- linux-amd64.tar.gz# sudo tar zxvf crictl-v1.25.0-linux-amd64.tar.gz -C /usr/local/bin If you run the following command it will fail with this message: (so, do not run this command):### minikube start --driver=none================================CHANGES BEGIN HERE:### = no longer valid commands; do not use### minikube start --driver=none### INSERT THESE COMMANDS INSTEAD# sudo mkdir -p /opt/cni/bin# sudo apt update# sudo apt install -y containernetworking-plugins# sudo apt install -y conntrack# sudo apt install -y socat ebtables ethtool# sudo minikube delete# sudo minikube start --driver=none --memory=2000 (I tried 1500, too low for the 1800 minimum)# sudo chown -R $USER $HOME/.kube $HOME/.minikube# kubectl get nodes============================================RESUME:# kubectl apply -f https://docs.projectcalico.org/manifests/calico-typha.yaml	      <--- this command will get your node to ready state (Installing calico-CNI)These commands should provide you with all the related errors to look for:-	kubectl describe <pod/node/any-resource> <resource-name> -n <namespace>-	kubectl logs <pod-name> -n <name-space>You should be good to go nowCheck that all pods are running: kubectl get pods -ACheck that your node is in "Ready" state: kubectl get nodes